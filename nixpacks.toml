[phases.setup]
nixPkgs = ["nodejs-18_x", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "tsx server/migrate.ts && tsx server/seed-if-empty.ts && NODE_ENV=production node dist/index.js"
